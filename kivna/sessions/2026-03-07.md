# Session — 2026-03-07

**Machine:** Mac.attlocal.net

## What Was Done
- Added mode markers to dian and skriv (v0.5.0) — visible phase/state announcements (`[dian: execute]`, `[skriv: active]`, etc.) with shared state file `kivna/.active-modes`
- Updated switch-in to read and report active modes
- Added commit rules to CLAUDE.md: always push after commit, always run release checklist first
- Attempted description optimization via skill-creator plugin — generated 20 trigger eval queries per skill, ran baseline eval, but automated improvement loop failed (needs ANTHROPIC_API_KEY). Baseline data inconclusive due to `claude -p` test harness limitations. Current descriptions work fine in practice.
- Strengthened dian (v0.6.0): consistency sniff test in orient, rigorous planning with pushback, verify-as-you-go in execute, immediate decision recording, docs-with-code enforcement, diff review in close-out
- Strengthened switch (v0.6.0): reflection step on switch-out (capture learnings to CLAUDE.md/memory/playbook), smoke test on switch-in (run project tests if they exist)

## Key Decisions
- Mode markers over a kerd:status skill — the real problem was not knowing when skills are active. Solved with self-announcing markers and a state file, not a new queryable skill.
- Only dian and skriv get markers — they're the only modal skills. One-shot skills don't need them.
- Description optimization deferred — current descriptions trigger correctly in real usage. The automated eval harness doesn't replicate real plugin triggering, so results aren't actionable.
- Decision recording belongs in execute, not close-out — decisions lose their reasoning context if deferred.

## Commits
- `8be920e` feat: add mode markers for dian and skriv
- `5efce4a` docs: add commit rules — always push, always run checklist first
- `55c8bc5` feat: strengthen dian planning/verification and add switch reflection

## Insights
- The skill-creator's `run_loop.py` uses `claude -p` with temp command files to test triggering, which doesn't replicate real plugin skill triggering behavior. Short `claude -p` invocations tend to answer directly without consulting skills. This means the eval harness is better suited for skills in other contexts (e.g., command-based skills) than for plugin skills loaded at session start.
- The user's frustration about not knowing when skills are active was a UX gap, not a feature gap. The fix was simpler than building a new skill.

## What's Next
- Test the strengthened dian in a real project session — see if the planning pushback and verify-as-you-go actually reduce rework
- Test switch-out reflection on a meaningful session
- Sotu playbook audit still untested on a project with a real playbook

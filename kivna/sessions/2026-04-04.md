# Session — 2026-04-04

**Machine:** Mac.attlocal.net

## What Was Done
- Explored shodh-memory repo for transferable patterns: hook-driven enrichment, orchestration via todo graphs, proactive context surfacing, portable memory format, token awareness
- Brainstormed 4 features through structured Q&A: Stop hook, SessionStart hook, PostToolUse mode reminder, KIF interchange format
- Wrote design doc at docs/plans/2026-04-04-hooks-and-kif-design.md
- Two rounds of code review on the design caught 7 issues: parameterized step matching, .active-modes schema, TOON import parser, hook wiring location, repeated steps, cross-machine scope, gitignore gap
- Built v0.19.0: 3 opt-in hooks (stop.sh, session-start.sh, skill-complete.sh), unified .active-modes schema across dian/skriv/mode/switch, structured mode steps format (stable IDs + concrete args), switch mode snapshot for cross-machine handoff, tend category 9 (hook hygiene), slainte release audit category
- Built v0.20.0: KIF format. Rewrote /kivna out to be repo-grounded (TODO, sessions, playbook, vault first, conversation fills gaps). Produces .kif.toon (LLM handoff) + .kif.json (machine import). Updated /kivna in to parse .kif.json with per-section approval
- Built v0.21.0: Lorg ranking (theme match + tech match + recency boost - friction, scored and sorted, weak matches dropped). Shared state contract doc at docs/state-contract.md defining ownership/format/rules for all cross-skill state surfaces
- Fixed P1: hook step matching used bare skill name prefix instead of concrete invocation. Replaced with exact match on skill + args
- Fixed P2: removed jq dependency from hooks (pure bash/sed). Fixed README/playbook doc drift (slainte areas, tend categories, kivna export format)
- Ran /kerd:tend: fixed 3 namespace prefix violations in kivna/SKILL.md and playbook.md, registered hooks in .claude/settings.local.json

## Key Decisions
- Hooks are opt-in (collaborator-local via settings.local.json), not plugin-level auto-activate
- PostToolUse hook is read-only (reminds, doesn't mutate .active-modes). Mode skill remains sole writer
- .active-modes gets a unified schema as prerequisite to hooks. Each skill owns its own line(s)
- KIF: export both TOON + JSON, import JSON only (no TOON parser needed)
- Exports are repo-grounded first, conversation fills gaps (reverses previous conversation-first approach)
- Lorg ranking uses simple additive score: theme + tech + recency - friction. Cutoff at 15 drops noise
- Stale usage threshold: 30 days (was "never invoked" which hid forgotten skills)

## Commits
- `2db6945` feat: hooks, unified .active-modes schema, slainte release audit (v0.19.0)
- `35f49e4` feat: Kerd Interchange Format — repo-grounded TOON + JSON export/import (v0.20.0)
- `00feb30` feat: lorg ranking, shared state contract (v0.21.0)
- `b94bc51` fix: hook step matching, jq dependency, doc drift (v0.21.0)

## What's Next
- Live smoke test of hooks (especially PostToolUse payload shape) in a fresh session
- Merge Kwanwoo's trim PR when he approves
- Run /kerd:tend on other repos for vault migration

---

# Session 2026-04-04 (session 2)

**Machine:** Mac.attlocal.net
**Branch:** `main`
**Tracking:** `origin/main (up to date)`

## What Was Done
- Live smoke test of all three hooks. SessionStart confirmed working (fired at conversation start). Stop confirmed via manual test. PostToolUse confirmed via real Skill tool invocation with payload capture to /tmp.
- Captured and documented the full PostToolUse payload shape: `{session_id, cwd, hook_event_name, tool_name, tool_input: {skill, args}, tool_response: {success, commandName}, tool_use_id}`. Added to state-contract.md and design doc.
- Hardened skill-complete.sh: added `tool_response.success` guard so failed skill runs don't trigger misleading "step complete" messages.
- Reviewed blader/humanizer against skriv. Added 4 rules to skriv (v0.22.0): self-audit pass before the 20% cut, synonym cycling ban, broadened copula avoidance ("serves as / functions as / acts as" where "is" works), chatbot residue cleanup.
- Reviewed thedotmack/claude-mem against kivna and switch. Added 3 features to switch (v0.23.0): branch metadata in session log template, first-class Gotchas section with explicit prompt in reflection, progressive session log loading on switch-in.
- Updated vault: Status.md (full rewrite from v0.17.1 to v0.23.0), MOC version bump, Weekly entry for this week, Architecture Decisions (hooks, KIF, switch, skriv decisions plus 4 rejected approaches), Usage Guide (switch and skriv sections).

## Key Decisions
- Skriv should NOT adopt voice calibration from humanizer. Skriv imposes an opinionated house style, not user mimicry. Different products.
- Skriv should NOT adopt blanket anti-passive-voice. Gets dumb fast in technical writing.
- Switch should NOT adopt automatic PostToolUse memory capture from claude-mem. Too noisy for Kerd's file-based philosophy. Session-boundary capture is the right granularity.
- PostToolUse hook should guard on success before printing progress. Prevents misleading "step complete" on failed skill runs.

## Commits
- `a81c960` fix: guard PostToolUse hook on tool_response.success, document payload shape
- `e5817d9` feat: skriv self-audit pass, synonym cycling, copula avoidance, chatbot residue (v0.22.0)
- `1ba7f58` feat: switch branch metadata, gotcha capture, progressive loading (v0.23.0)

## Gotchas
- The cached plugin version (0.21.0) doesn't update until the user reinstalls. The switch template loaded from cache during this session was the old one without branch/gotcha fields. Changes to skills only take effect after `claude plugins install kerd`.
- The PostToolUse payload is a full envelope, not just tool_input. The sed parser works by accident (greedy match finds "skill" anywhere), but this should be understood as intentional resilience, not luck.

## Insights
- Reviewing external projects (humanizer, claude-mem) is a good source of specific, actionable improvements. The pattern is: read their approach, identify what maps to Kerd's philosophy, reject what doesn't, add the 3-4 things that fit.
- The hooks design is now fully validated end-to-end. The payload shape documentation closes the last open question from the v0.19.0 design.

## What's Next
- Merge Kwanwoo's trim PR when he approves
- Run /kerd:tend on other repos to migrate vaults
- Decide on AGENTS.md and demo media files

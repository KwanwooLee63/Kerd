# Session — 2026-04-07

**Machine:** Mac.attlocal.net
**Branch:** main
**Tracking:** origin/main (up to date)

## What Was Done
- Fixed broken hook paths in leru (same ${CLAUDE_PLUGIN_ROOT} issue as krutho-founders/krutho-strategy). Scanned all repos — no more broken paths remain.
- Evaluated MrLesk/Backlog.md (5.3k stars, markdown-native task manager with CLI/MCP/web UI). Decided to borrow patterns, not the tool. Added to lorg curated sources (discover-sources.json in vault).
- Built v0.30.0: switch `low` modifier. Minimum viable handoff for tight token budgets. Brief TODO (3-5 lines), skeleton session log (What Was Done + What's Next), skip vault/reflection/triage, compressed narration. Three-level progression: full → light → low.
- Built v0.31.0: dian task framing. Plan phase now starts with decompose → approve boundaries → write implementation plan. Each task gets scope, acceptance criteria, files touched, verification. Default one task per session. Fresh-session retry when framing was wrong.
- Consolidated README What's New from 10 per-version entries into 8 skill-grouped summaries.
- Deleted 5 legacy user commands from ~/.claude/commands/ (switch, kivna, sotu, human-draft, rigour) that were shadowing plugin skills in the command picker.

## Key Decisions
- Backlog.md not adopted as integration. It's a TypeScript CLI (violates "no runtime"), creates overlap with TODO.md, and its MCP server would compete with dian for task creation. But its task framing pattern (describe → decompose → approve → plan → implement) was worth borrowing.
- Switch `low` chosen over Kwan's bash scripts. The scripts automate the 20% of switch that's mechanical and lose the 80% that's valuable. Low mode keeps the judgment while compressing everything else.
- No CLI version of switch. The `low` modifier is the Kerd-shaped answer to token budget pressure.
- Legacy user commands deleted — all 5 were pre-plugin leftovers superseded by kerd:* skills.

## Commits
- `1d14ace` feat: switch low modifier for minimum viable handoffs (v0.30.0)
- `47d4c99` feat: dian task framing — decompose before planning (v0.31.0)
- `56fc3a9` docs: consolidate What's New, update dian description for task framing

## Gotchas
- Cached plugin version (0.23.1) doesn't reflect any of the v0.24.0–v0.31.0 improvements until `claude plugins install kerd` is run. The switch skill loaded from cache during this session was the old version without low modifier, pre-commit summary, or task framing.
- Legacy ~/.claude/commands/ files shadow plugin skills in the command picker, showing two entries for the same command. Any pre-plugin command files should be deleted when migrating to the plugin.

## Insights
- The "borrow patterns, not tools" approach continues to be the highest-value move. Backlog.md's task framing pattern became dian v0.31.0 without importing a task management system. Same pattern as humanizer → skriv and claude-mem → switch.
- Three modifier levels (full/light/low) give users explicit control over the completeness-vs-cost trade-off. Better than scripts because the skill still owns the judgment at every level.

## What's Next
- Run `claude plugins install kerd` to pick up v0.24.0–v0.31.0 in the cache
- Run /kerd:tend on krutho-founders, krutho-strategy, obair to migrate vaults
- Solicit community mode contributions
